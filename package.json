{
  "name": "github-stats-dashboard",
  "version": "1.0.0",
  "description": "Self-hosted GitHub stats: generates SVG cards + a stats.json dashboard, zero dependencies.",
  "type": "module",
  "scripts": {
    "build": "node scripts/fetch-stats.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
